# NoteCodeApi

<!-- cmd to run server and allow all reseau interface -->
dotnet watch run --urls="http://0.0.0.0:5177"

<!-- A executer si dotnet ef ne marche pas -->
❯ export PATH="$PATH:/home/cesar/.dotnet/tools"